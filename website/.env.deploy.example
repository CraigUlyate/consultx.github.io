# Afrihost deploy credentials — never commit

AFRIHOST_HOST=major.aserv.co.za
AFRIHOST_USER=consuvuv
AFRIHOST_PASSWORD=
AFRIHOST_PORT=21
AFRIHOST_PROTOCOL=ftp
# FTP is chrooted to the account home. Use paths relative to that root:
#   /public_html          <- live ConsultX site (WordPress was here)
#   /consultx.co.za       <- separate folder (has a wordpress subfolder)
AFRIHOST_REMOTE_DIR=/public_html
